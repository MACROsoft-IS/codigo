package es.ucm.fdi.negocio;

import java.util.ArrayList;

import es.ucm.fdi.integracion.TOUsuario;
import es.ucm.fdi.negocio.Pendiente.tPendiente;

public class BOUsuario {
	/*
	public void añadirConocido(String correo, TOUsuario usuario){
		ArrayList<String> lista = usuario.getListaConocidos();
		lista.add(correo);
		usuario.setListaConocidos(lista);
	}
	
	public void eliminarConocido(String correo, TOUsuario usuario){
		//ASI FUNCIONARIA????
		usuario.getListaConocidos().remove(correo);
	}
	
	public void añadirPendiente(String correo, tPendiente tipo,TOUsuario usuario){
		usuario.getListaPendientes().add(new Pendiente(correo, tipo));
	}
	
	public void eliminarPendiente(String correo,TOUsuario usuario){
		
		int i = 0;
		boolean eliminado = false;
		
		while (i < usuario.getListaPendientes().size() && !eliminado){
			if (usuario.getListaPendientes().get(i).getCorreo().equalsIgnoreCase(correo)) {
				usuario.getListaPendientes().remove(i);
				eliminado = true;
			}
			else i++;
		}
	}
	
	public void añadirChat(String correo, TOUsuario usuario){
		usuario.getListaChats().add(new Chat(correo));
	}
	
	public void eliminarChat(String correo, TOUsuario usuario){
		int i = 0;
		boolean eliminado = false;
		
		while (i < usuario.getListaChats().size() && !eliminado){
			if (usuario.getListaChats().get(i).getCorreo().equalsIgnoreCase(correo)) {
				usuario.getListaChats().remove(i);
				eliminado = true;
			}
			else i++;
		}
	}
	
	public void addAviso(String aviso, TOUsuario usuario){
		usuario.setAvisos(usuario.getAvisos() + aviso);
	}

	public String toString(TOUsuario usuario) {
		return "Aqui se mostraria la foto de perfil" + System.getProperty("line.separator") +
				usuario.getNombre() + System.getProperty("line.separator") +
				usuario.getDescripcion() + System.getProperty("line.separator");
	}
	
	
	public String showPendientes(TOUsuario usuario){
		String string = "";
		for(int i = 0; i < usuario.getListaPendientes().size(); i++){
			string = string + usuario.getListaPendientes().get(i).getCorreo() + " ";
			
		}
		return string;
	}
	public String showChats(TOUsuario usuario){
		String string = "";
		for(int i = 0; i < usuario.getListaChats().size(); i++){
			string = string + usuario.getListaChats().get(i).getCorreo() + " ";
			
		}
		return string;
	}
	public String showConocidos(TOUsuario usuario){
		String string = "";
		for(int i = 0; i < usuario.getListaConocidos().size(); i++){
			string = string + usuario.getListaConocidos().get(i) + " ";
			
		}
		return string;
	}
	
	
	*/
	
	
		
		
	
	
	
	
		
	
}
